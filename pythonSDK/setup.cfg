[metadata]
name = perpex-sdk
version = 0.1.0
description = Python SDK port of the Perpex TS SDK (query-first)

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.9
install_requires =
    requests>=2.32.0
    web3>=7.0.0

[options.packages.find]
where = src

